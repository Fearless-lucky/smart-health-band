# 智能健康手环 · 完整技术文档

> 一份让完全不懂嵌入式的人，也能看懂每一行代码的文档。
>
> **阅读建议**：从头读到尾。前面用比喻建立直觉，后面逐文件拆解。
> 遇到看不懂的地方，回头看"第 0 章 前置知识"。

---

## 目录

- [第 0 章 写在前面：必须先懂的几个概念](#第-0-章-写在前面必须先懂的几个概念)
- [第 1 章 这个项目是做什么的](#第-1-章-这个项目是做什么的)
- [第 2 章 整体架构鸟瞰](#第-2-章-整体架构鸟瞰)
- [第 3 章 硬件接线大全](#第-3-章-硬件接线大全)
- [第 4 章 程序运行的"心脏"：FreeRTOS 四个任务](#第-4-章-程序运行的心脏freertos-四个任务)
- [第 5 章 逐文件精讲（.c 源文件）](#第-5-章-逐文件精讲c-源文件)
- [第 6 章 逐文件精讲（.h 头文件）](#第-6-章-逐文件精讲h-头文件)
- [第 7 章 核心难点：计步、心率、血氧算法](#第-7-章-核心难点计步心率血氧算法)
- [第 8 章 所有可调参数速查表](#第-8-章-所有可调参数速查表)
- [第 9 章 编译、烧录、调试](#第-9-章-编译烧录调试)
- [第 10 章 已知问题与改进方向](#第-10-章-已知问题与改进方向)
- [附录 A：术语表](#附录-a术语表)
- [附录 B：文件清单](#附录-b文件清单)

---

## 第 0 章 写在前面：必须先懂的几个概念

这一章是给完全没接触过单片机/嵌入式的人看的。如果你已经懂，可以跳到第 1 章。

### 0.1 什么是"单片机"

单片机（Microcontroller，MCU）就像一台超迷你的电脑，把 CPU、内存（RAM）、硬盘（Flash）全塞进一颗芯片里。

本项目用的芯片是 **STM32F103ZE**（Cortex-M3 高密度 / High-density 系列），参数：

| 指标 | 数值 | 类比 |
|------|------|------|
| CPU 主频 | 72 MHz | 比 Arduino 快 ~5 倍 |
| RAM（运行内存） | 64 KB | 比入门款 20KB 宽裕不少 |
| Flash（程序存储） | 512 KB | 程序烧在这里，掉电不丢 |
| 内核 | ARM Cortex-M3 | 32 位，跟手机同宗 |
| 封装 | LQFP-144 | 144 脚，与 STM32F103 家族同源 |

**关键认知**：64KB 内存意味着——你不能像写 Python 那样随意 `new` 大对象、随便用 `printf`。每一段内存都要精打细算。

### 0.2 什么是"寄存器"和"GPIO"

**寄存器（Register）** 就是芯片内部的一个个小开关，每个开关控制芯片的一个功能。你可以把它想象成一排**物理开关墙**：

```
寄存器 GPIOA_CRL 的 32 个 bit（开关）：
bit:  31 30 29 ... 4  3  2  1  0
状态:  0  0  0      0  1  0  1  0
                              ↑ 这一位控制某个引脚是输入还是输出
```

**GPIO（通用输入输出）** 是芯片伸出来的"金属脚"，能输出高/低电压（3.3V 或 0V），也能读外部的电压。

- **输出**：让某个脚变成 3.3V（高电平）或 0V（低电平），用来控制 LED、给传感器发信号。
- **输入**：读某个脚当前是 3.3V 还是 0V，用来读按键、读传感器返回的数据。

本项目的 GPIO 引脚代号像 `PA0`、`PB8`。`PA` 表示 A 组，`PB` 表示 B 组。

### 0.3 什么是"通信协议"：I2C / UART / 1-Wire

传感器不会说话，它们用"约定的暗号"和芯片通信。本项目用了三种暗号：

#### I2C（读作"I-squared-C"）
**两条线**（SCL 时钟 + SDA 数据）挂多个设备，像**公交线**。
- 多个设备（MAX30102、MPU6050、OLED）可以挂在同一条 I2C 总线上。
- 每个设备有唯一"门牌号"（地址），主芯片喊"0x57 号，交数据"。
- 本项目用两条独立 I2C 总线：I2C1 接传感器，I2C2 接屏幕。

#### UART（串口）
**两条线**（TX 发 + RX 收），像**打电话**，一对一聊。
- 用途：连蓝牙模块 HC-05、连语音模块 ASR、以及调试打印。
- 讲究"波特率"（每秒传多少 bit），两边必须一致，比如 9600 或 115200。

#### 1-Wire（单总线）
**一条线**（+ 地线），像**对讲机**，半双工。
- DS18B20 温度传感器用这个，数据在一条线上双向传输。
- 时序要求非常严格（微秒级），差几微秒就读错。

### 0.4 什么是"中断"和"任务"

裸机程序通常是 `main()` 里一个死循环，从头到尾跑。但真实世界需要"同时做多件事"：

- **中断（Interrupt）**：硬件突然发生事件（比如按键按下、串口收到字节），CPU 暂停当前工作，跳去处理，处理完回来。**像电话铃响**，必须立刻接。

- **任务（Task）/ RTOS**：把程序分成几个独立的"小循环"，由"调度器"轮流让它们跑，看起来像同时运行。本项目用 **FreeRTOS**，它就是那个"调度员"。

  ```
  时间 →
  任务A（传感器）: ████    ████    ████
  任务B（显示）  :     ████    ████
  任务C（蓝牙）  :          ██      ██
                  ↑ 调度器快速切换，看起来像"同时"
  ```

### 0.5 什么是"看门狗"

看门狗（Watchdog）是一只**定时炸弹**：
- 启动后开始倒计时（本项目 4 秒）。
- 程序必须定期"喂狗"（重置倒计时）。
- 如果程序卡死了（比如死循环、总线锁死），没人喂狗，倒计时归零 → **芯片自动重启**。

这是嵌入式系统的最后一道防线，保证系统死机了也能自己活过来。

### 0.6 什么是 `.c` 和 `.h` 文件

- `.c`（源文件）：放**真正的代码**（函数的实现）。
- `.h`（头文件）：放**声明**（告诉别的文件"我这里有这些函数可以用"），相当于菜单。

别的 `.c` 想用某个函数，就 `#include` 对应的 `.h`，就像"翻开菜单点菜"。

---

## 第 1 章 这个项目是做什么的

**一句话**：做一个戴手腕上的健康手环，能测心率、血氧、步数、体温，还能用语音/蓝牙控制。主板型号 STM32F103ZE，跑 FreeRTOS，4 个任务并发。

### 它能干什么（功能清单）

| 功能 | 怎么触发 | 用什么传感器 |
|------|----------|--------------|
| 测心率 (HR) | 自动 / 语音"查看心率" | MAX30102（夹指/贴皮肤） |
| 测血氧 (SpO₂) | 自动 / 语音"查看血氧" | MAX30102 |
| 计步 | 自动（一直计） | MPU6050（加速度计） |
| 体温 | 自动 / 语音"查看体温" | DS18B20 + MPU6050 补偿 |
| 看时间 | 主页一直显示 / 语音"查看时间" | DS1302（时钟芯片） |
| 翻页 | 按键 WK_UP / 语音"下一页" | — |
| 蓝牙上传 | 每 2 秒自动发 JSON | HC-05 |
| 蓝牙校准 | 发 `CAL` 指令 | HC-05 |
| 蓝牙对时 | 发 `TIME` 指令 | HC-05 |

### 屏幕（OLED）有 5 个页面，按键 WK_UP 循环切换；语音 "下一页" 也能翻页

```
主页（时间+语音指令指南） → 心率 → 血氧 → 步数 → 体温 → 回主页...
```

另外 ASR PRO 还能**直接跳转**到指定页面（语音指令单字节码）：
- "查看心率" → 跳到心率页
- "查看步数" / "查看血氧" / "查看体温" / "查看时间" → 跳到对应页
- "归零步数" → 清零 + 显示步数页
- "下一页" → 循环翻页（2 秒防抖）
- "运动状态" → 显示活动识别页（Walking / Running / Shaking / Resting）

### 一句话总结整个流程

> 芯片每时每刻从 4 个传感器采集数据 → 算法把原始数据变成有意义的心率/血氧/步数/体温 → 显示在屏幕上 + 通过蓝牙发出去，同时听按键和语音的指令切换页面。

---

## 第 2 章 整体架构鸟瞰

把项目想成一栋**小公司**，每个角色各司其职：

```
┌─────────────────────────────────────────────────────────┐
│                    main.c（总经理）                       │
│         开机时初始化所有部门，然后启动调度器              │
└─────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │传感器层 │        │ 算法层  │        │ 显示层  │
   │(驱动)   │  原始   │(大脑)   │ 结果   │(屏幕)   │
   │         │──数据──→│         │───────→│         │
   └─────────┘        └─────────┘        └─────────┘
        │                  │                  │
        ▼                  ▼                  ▼
   MAX30102             心率/血氧            OLED
   MPU6050              计步                 SSD1306
   DS18B20              活动识别
   DS1302               
   
   ┌─────────┐        ┌─────────┐
   │ 通信层  │        │ 系统层  │
   │(嘴巴耳朵)│        │(基础设施)│
   └─────────┘        └─────────┘
   HC-05 蓝牙          FreeRTOS（调度器）
   ASR 语音            IWDG（看门狗）
   UART/I2C HAL       Systick（时钟）
```

### 数据是怎么流动的（以"测心率"为例）

```
[手指贴上 MAX30102]
      ↓ 红光/红外光被血液吸收
[MAX30102 芯片] 输出原始光强数字（IR/Red 各 18 位）
      ↓ I2C1 传输
[驱动 max30102.c] 读 FIFO，把字节拼成整数
      ↓ 调用 PPG_ProcessSamples
[算法 algorithms.c] 滤波 → 找波峰 → 算间隔 → 得心率
      ↓ 写入全局变量 hr
[显示 oled_ssd1306.c] 读 hr，画到屏幕
[蓝牙 hc05.c] 读 hr，包成 JSON 发出去
```

### 文件分三大类

| 类别 | 文件 | 作用 |
|------|------|------|
| **驱动层** | `max30102.c` `mpu6050.c` `ds18b20.c` `ds1302.c` | 直接和某个传感器对话 |
| **通信层** | `i2c.c` `uart.c` `hc05.c` `asr_pro.c` | 负责数据传输 |
| **逻辑层** | `algorithms.c` `oled_ssd1306.c` `main.c` | 算法、显示、任务编排 |

加上 **系统层**：`systick.c`（延时/计时）和 `iwdg.c`（看门狗，已并入 `main.c`）。

---

## 第 3 章 硬件接线大全

这是整个项目的"地图"，**接错线什么都跑不起来**。

### 3.1 引脚总表

| 引脚 | 功能 | 连接到 | 配置模式 |
|------|------|--------|----------|
| PA0  | 按键 WK_UP | 按键到 VCC | 开漏输出+低（模拟下拉） |
| PA1  | 1-Wire 数据 | DS18B20 DQ | 输入上拉 / 推挽（动态切换） |
| PA2  | UART2 TX | ASR 模块 RX | 复用推挽 |
| PA3  | UART2 RX | ASR 模块 TX | 上拉输入 |
| PA9  | UART1 TX | HC-05 RX / 调试 | 复用推挽 |
| PA10 | UART1 RX | HC-05 TX / 调试 | 上拉输入 |
| PB1  | 时钟 | DS1302 CLK | 推挽输出 |
| PB8  | I2C1 SCL | MAX30102 + MPU6050 | 复用开漏（重映射） |
| PB9  | I2C1 SDA | MAX30102 + MPU6050 | 复用开漏（重映射） |
| PB10 | I2C2 SCL | OLED 屏幕 | 复用开漏 |
| PB11 | I2C2 SDA | OLED 屏幕 | 复用开漏 |
| PB13 | 片选 | DS1302 CE | 推挽输出 |
| PB14 | 数据 | DS1302 DAT | 推挽输出/上拉输入（切换） |

### 3.2 I2C 地址表

每个 I2C 设备有 7 位地址，代码里用的是"8 位形式"（地址左移 1 位，最低位表示读/写）：

| 设备 | 7位地址 | 代码里的值 | 在哪定义 |
|------|---------|-----------|----------|
| MAX30102 | 0x57 | `0x57 << 1 = 0xAE` | max30102.h |
| MPU6050 | 0x68 | `0x68 << 1 = 0xD0` | mpu6050.h |
| OLED SSD1306 | 0x3C | `0x3C << 1 = 0x78` | oled_ssd1306.c |

### 3.3 ⚠️ 重要提醒

- **DS18B20 需要一个 4.7kΩ 上拉电阻**接到 3.3V，否则总线拉不高，读不到数据。
- **DS1302 模块通常自带晶振和上拉**，不用外加。
- **PB8/PB9 用了 I2C1 重映射**（默认是 PB6/PB7），这是在 `i2c.c` 里 `GPIO_PinRemapConfig(GPIO_Remap_I2C1, ENABLE)` 做的。

---

## 第 4 章 程序运行的"心脏"：FreeRTOS 四个任务

开机后，`main()` 初始化所有硬件，然后创建 4 个"任务"，交给 FreeRTOS 调度。这 4 个任务**并发运行**，是整个程序的主角。

### 4.1 四个任务一览

| 任务名 | 函数 | 栈大小 | 优先级 | 周期 | 干什么 |
|--------|------|--------|--------|------|--------|
| Sensors | vTaskSensors | 512字(2KB) | 3（最高） | 200ms | 读所有传感器，喂狗 |
| Display | vTaskDisplay | 512字(2KB) | 2 | 50ms | 刷新 OLED 屏幕 |
| Voice | vTaskVoice | 384字(1.5KB) | 2 | 100ms | 处理语音模块指令 |
| BT | vTaskBluetooth | 384字(1.5KB) | 2 | 20ms | 蓝牙收发 |

### 4.2 为什么要分任务？

想象你一个人要同时：测心率、刷屏幕、听蓝牙、听语音。如果串行做，测心率（要等 750ms 温度转换）期间屏幕就卡死了。

拆成 4 个任务后，FreeRTOS 让它们**轮流跑**，每个任务"专注自己的事，做完就睡一会儿（vTaskDelay）"，让出 CPU 给别的任务。

### 4.3 优先级的意义

优先级高的任务可以**抢占**低的。本项目 Sensors 优先级最高（3），因为它要按时喂狗（否则系统重启）且要严守传感器时序。其余都是 2。

### 4.4 任务之间怎么"对话"？

通过**全局变量**（带锁保护）：

```c
volatile float g_temperature;   // 体温
volatile int   g_temp_valid;    // 体温是否有效
volatile int   g_page_advance;  // 翻页请求（语音→显示）
```

读体温用专门的 `Get_Temperature()`（内部加锁，保证读到的"有效标志"和"温度值"是配套的，不会出现"有效=1 但温度是上次的旧值"这种撕裂）。

---

## 第 5 章 逐文件精讲（.c 源文件）

这是文档的核心。每个文件按"**它干啥 → 关键代码逐段讲 → 易错点**"展开。

---

### 5.1 `main.c` —— 总指挥

`main()` 是开机后第一个跑的函数。它的工作分 10 步，像一份开机清单：

#### 第 1 步：系统内核初始化
```c
SystemInit();              // 配置时钟树，让 CPU 跑到 72MHz
SystemCoreClockUpdate();   // 更新全局变量 SystemCoreClock = 72000000
Systick_Init();            // 启动 DWT 周期计数器（用于微秒级精确延时）
```
**比喻**：给电脑通电、设置 CPU 主频、准备秒表。

#### 第 2 步：通信总线初始化
```c
I2C_HAL_Init();   // I2C1 (PB8/PB9) → MAX30102 + MPU6050
I2C2_Init();      // I2C2 (PB10/PB11) → OLED
UART1_Init(115200); // UART1 (PA9/PA10) → 调试 + HC-05
```

#### 第 3~5 步：算法、传感器、外设初始化
按依赖顺序初始化：先算法（PPG_Init），再传感器，再显示/按键/蓝牙/语音。

#### 第 6~7 步：DS1302 自检 + 打印时间
开机时通过 UART1 打印 RTC 状态，方便你**用串口工具看到调试信息**，确认时钟芯片是否正常。

#### 第 8 步：创建 4 个任务
```c
xTaskCreate(vTaskSensors,   "Sensors", 512, NULL, 3, NULL);
xTaskCreate(vTaskDisplay,   "Display", 512, NULL, 2, NULL);
xTaskCreate(vTaskVoice,     "Voice",   384, NULL, 2, NULL);
xTaskCreate(vTaskBluetooth, "BT",      384, NULL, 2, NULL);
```
参数含义：`(函数, 名字, 栈大小(字), 参数, 优先级, 句柄)`。

#### 第 9 步：启动看门狗（4 秒超时）
```c
IWDG_Init();
```
**注意**：一旦开启，无法关闭。如果后面调度器没启动，4 秒后自动重启。

#### 第 10 步：启动调度器
```c
vTaskStartScheduler();  // 这一行之后，4 个任务开始轮流跑
for (;;) {}              // 永远不会执行到这里（除非内存不够创建空闲任务）
```

#### 两个"救命"钩子函数

- **`vApplicationStackOverflowHook`**：当某个任务栈溢出（用了超出分配的内存），打印任务名后死循环。**像保险丝烧断**，告诉你哪个任务爆栈了。
- **`Assert_Handler`**：FreeRTOS 内部检查失败时调用，打印文件名行号后死循环 → 看门狗 4 秒后重启。

#### vTaskSensors（传感器任务）精讲

```c
for (;;) {
    IWDG_Feed();              // ① 每轮先喂狗，防止超时重启

    MAX30102_ReadData(NULL, NULL, &count);  // ② 读心率血氧 FIFO
    // 传 NULL：数据已在驱动内部送进算法，不用再拷贝，省内存

    if (MPU6050_ReadData(...) == 0) {       // ③ 读加速度+芯片温度
        Step_ProcessAccel(ax, ay, az);     //    喂给计步算法
        mpu_ok = 1;
    }

    if (ds18b20_tick == 0) {                // ④ DS18B20 两步操作的第一步
        DS18B20_StartConversion();          //    启动温度转换（需 ≥750ms）
    } else if (ds18b20_tick >= 5) {         //    第 5 轮（5×200ms=1000ms 后）读结果
        if (DS18B20_ReadData(&skin_temp) == 0) {
            g_temperature = Compensate_Temperature(skin_temp, chip_temp);
            g_temp_valid = mpu_ok;          // MPU 不在线就不标有效
        }
        ds18b20_tick = 0;
        continue;                           // 不等 200ms，立刻下一轮
    }
    ds18b20_tick++;
    vTaskDelay(pdMS_TO_TICKS(200));         // 睡 200ms
}
```

**为什么 DS18B20 要分两步、隔 5 轮？** 因为它启动转换后**需要 750ms 才出结果**，不能立刻读。任务每轮 200ms，从启动到第 5 轮（5 × 200ms = 1000ms）才读，足够 12-bit 转换完成且留有 250ms 余量。

**为什么 `continue` 跳过 delay？** 读完后立刻开始下一轮转换，让采样更密，不浪费这 200ms。

#### vTaskDisplay（显示任务）精讲

```c
int page = 0;                              // 当前页
int tick = DISPLAY_FORCE_REFRESH;          // 复位后立即刷首页

for (;;) {
    if (Key_Get() || g_page_advance) {     // 按键或语音要翻页
        page = (page + 1) % 5;             // 0→1→2→3→4→0 循环
        g_page_advance = 0;
        tick = DISPLAY_FORCE_REFRESH;      // 强制刷新
    }

    if (tick >= DISPLAY_REFRESH_CYCLES) {  // 每 10 轮（500ms）刷一次
        tick = 0;
        switch (page) {
            case 0: OLED_ShowMainPage();   break;
            case 1: OLED_ShowHeartRate(Get_HeartRate()); break;
            case 2: OLED_ShowSpO2(Get_SpO2());           break;
            case 3: OLED_ShowSteps(Get_StepCount());     break;
            case 4: OLED_ShowTemperature();              break;
        }
    }
    tick++;
    vTaskDelay(pdMS_TO_TICKS(50));         // 50ms 一轮
}
```

**为什么 500ms 才刷一次？** 刷一帧 OLED 要约 30ms（I2C 传 1024 字节），如果 50ms 刷一次太频繁，会霸占总线、增加功耗。500ms 刷一次，肉眼完全感觉不到延迟。

**DISPLAY_FORCE_REFRESH = 16**：故意设得比 `DISPLAY_REFRESH_CYCLES(10)` 大，保证翻页后**立刻**进入刷新分支。

---

### 5.2 `i2c.c` —— I2C 总线管家

负责 I2C1 和 I2C2 的所有读写。是整个项目**最底层、最容易出 bug** 的文件之一。
名字已去掉名不符实的 `_hal` 后缀（实际用的是 StdPeriph 库，不是 HAL 库）。

#### 核心设计：每次出错自动复位总线

```c
static int i2c_xfer(...) {
    if (i2c_lock(mtx) != 0) return -1;     // 加锁（防多任务同时用总线）

    // ... 发 START、地址、数据 ...

out:
    if (ret != 0) {                        // 如果出错
        I2C_GenerateSTOP(i2c, ENABLE);     // 发 STOP 释放总线
        i2c_bus_reset(i2c);                // 复位整条总线（关键！）
    }
    i2c_unlock(mtx);
    return ret;
}
```

**为什么这是亮点？** STM32F1 的硬件 I2C 出了名的"容易卡死"（总线 BUSY 标志清不掉）。这个设计让 I2C 一旦出错就 DeInit+ReInit，**自愈**，不用拔电池。

#### I2C 读 1 字节 vs 读多字节的不同

读 1 字节时，必须在收到 ADDR 后**立刻关 ACK + 发 STOP**，否则会多读一个字节：
```c
if (len == 1) {
    I2C_AcknowledgeConfig(i2c, DISABLE);  // 关应答
    (void)i2c->SR2;                       // 清 ADDR
    I2C_GenerateSTOP(i2c, ENABLE);        // 发 STOP
    // 然后才读数据
}
```

读多字节时，在**最后一个字节之前**关 ACK + STOP：
```c
for (uint16_t i = 0; i < len; i++) {
    if (i == len - 1) {                   // 最后一个
        I2C_AcknowledgeConfig(i2c, DISABLE);
        I2C_GenerateSTOP(i2c, ENABLE);
    }
    i2c_wait(i2c, I2C_SR1_RXNE, 50);
    data[i] = I2C_ReceiveData(i2c);
}
```

#### 两条总线各有自己的锁
```c
static SemaphoreHandle_t g_i2c_mutex  = NULL;  // I2C1 的锁
static SemaphoreHandle_t g_i2c2_mutex = NULL;  // I2C2 的锁
```
两个任务（Sensors 用 I2C1，Display 用 I2C2）互不干扰。

---

### 5.3 `uart.c` —— 串口管家

管理 UART1（蓝牙+调试）和 UART2（语音）。
已去掉 `_hal` 后缀，与 `i2c.c` 保持一致。
#### 发送时的"每字节独立超时"
```c
for (uint16_t i = 0; i < len; i++) {
    uint32_t t0 = Systick_GetTick();
    USART_SendData(usart, data[i]);
    while (USART_GetFlagStatus(usart, USART_FLAG_TXE) == RESET) {
        if ((Systick_GetTick() - t0) > 200) return -1;  // 每字节 200ms 超时
    }
}
```
**为什么不设一个总超时？** 如果设"整帧 200ms"，前几个字节卡住就把预算耗光了，后面的字节来不及发就超时。每字节独立算，更公平。

#### ⚠️ 这里没有接收逻辑

UART 的**接收**放在了 `hc05.c` 和 `asr_pro.c` 里（用轮询读 `RXNE` 标志），没用中断。好处是简单，坏处是如果任务延迟太久，可能漏数据。本项目轮询周期 20ms，够用。

---

### 5.4 `max30102.c` —— 心率血氧传感器驱动

MAX30102 是个光电传感器：往手指照红光和红外光，测反射回来的光强变化，从中提取脉搏波。

#### 初始化流程
```c
if (read_reg(REG_PART_ID) != 0x15) return -1;  // 先验证芯片在线（ID=0x15）
write_reg(REG_MODE_CONFIG, 0x40);               // 软复位
write_reg(REG_FIFO_CONFIG, MAX30102_FIFO_CONFIG); // FIFO 滚动覆盖模式
write_reg(REG_SPO2_CONFIG, MAX30102_SPO2_CONFIG); // 100Hz 采样、4096 量程
write_reg(REG_LED1_PA, MAX30102_LED1_CURRENT);    // IR LED 电流
write_reg(REG_LED2_PA, MAX30102_LED2_CURRENT);    // Red LED 电流
write_reg(REG_MODE_CONFIG, 0x03);                 // 进入 SpO2 双 LED 模式
```

#### 读数据：从 FIFO 批量读
```c
uint8_t wr = read_reg(REG_FIFO_WR_PTR);  // 芯片写到哪了
uint8_t rd = read_reg(REG_FIFO_RD_PTR);  // 我们读到哪了
int samples = (wr - rd) & 0x1F;          // 可读样本数（0~31）

uint8_t raw[192];                        // 32×6 字节
I2C_ReadReg(..., REG_FIFO_DATA, raw, samples * 6);

for (int i = 0; i < samples; i++) {
    int32_t r  = (raw[idx]<<16) | (raw[idx+1]<<8) | raw[idx+2];   // 红 18位
    int32_t ri = (raw[idx+3]<<16) | (raw[idx+4]<<8) | raw[idx+5]; // 红外 18位
    red_dec[i] = r  & 0x3FFFF;   // 取低 18 位
    ir_dec[i]  = ri & 0x3FFFF;
}

PPG_ProcessSamples(ir_dec, red_dec, samples);  // 喂给算法
```

**`& 0x3FFFF` 是什么？** 0x3FFFF = 二进制 18 个 1，作用是"只取低 18 位"，因为 ADC 是 18 位的，高位是 0 或保留位。

---

### 5.5 `mpu6050.c` —— 加速度+陀螺仪驱动

MPU6050 用来**计步**和**测芯片温度**（给体温补偿做参考）。

#### 一次读 8 字节，拿到 3 轴加速度 + 温度
```c
I2C_ReadReg(MPU6050_I2C_ADDR, 0x3B, raw, 8);
// raw[0:1] = X 加速度（高字节在前）
// raw[2:3] = Y
// raw[4:5] = Z
// raw[6:7] = 温度
*ax = (int16_t)((raw[0] << 8) | raw[1]);  // 拼成 16 位有符号
```

**`int16_t` 强转**：加速度可以是负数（重力反向），所以用有符号 16 位。两个字节拼起来后强转回 `int16_t`，负数自动还原。

#### 温度换算
```c
*chip_temp = MPU6050_TEMP_OFFSET + (float)temp_raw / MPU6050_TEMP_SCALE;
// = 36.53 + raw / 340
```
这是 MPU6050 数据手册给的公式。

---

### 5.6 `ds18b20.c` —— 单总线温度传感器驱动

DS18B20 用 **1-Wire 协议**，时序极其严格（微秒级），是**全项目最脆弱**的代码。

#### 关键时序（复位脉冲）
```c
static int ow_reset(void) {
    dq_low();      // 拉低
    Delay_us(500); // 保持 500μs（≥480μs）
    dq_high();     // 释放
    Delay_us(60);  // 等 60μs
    dq_input();
    uint8_t presence = dq_read();  // 读：低=有设备响应
    Delay_us(420); // 等剩余时间
    return (presence == 0) ? 0 : -1;
}
```

**比喻**：你大喊一声（拉低 500μs），然后安静听（释放），如果总线上有 DS18B20，它会在 60μs 后回拉低一下（"我在！"），这就是 presence 脉冲。

#### 为什么全程在 `taskENTER_CRITICAL()` 里？
1-Wire 时序是微秒级，**任何中断打断都会让时序错乱**。所以必须关中断（进临界区），让 CPU 全神贯注。

#### CRC 校验
```c
if (ow_crc8(sp, 9) != 0) return -1;  // 9 字节cratchpad，CRC 应为 0
```
读出来的 9 字节，最后一字节是 CRC。如果 CRC 不对，说明传输被干扰，丢弃这次数据。**这是数据可靠性的保障**。

---

### 5.7 `ds1302.c` —— 实时时钟驱动

DS1302 是个独立的时钟芯片，**自带电池**（或超级电容），主芯片断电后它还在走。

#### 棘手点 1：BCD 编码
DS1302 存的数字是 **BCD 码**（Binary-Coded Decimal），不是普通二进制。
- 数字 23 存成 `0x23`（不是 `0x17`）。
- 所以要转换：
```c
static uint8_t dec2bcd(uint8_t v) { return ((v/10)<<4) | (v%10); }  // 23 → 0x23
static uint8_t bcd2dec(uint8_t v) { return (v>>4)*10 + (v&0x0F); }  // 0x23 → 23
```

#### 棘手点 2：CH 位（时钟停振）
```c
uint8_t sec = read_byte();
if (sec & 0x80) {   // CH 位=1，晶振没在振荡
    write_byte(0x80, sec & 0x7F);  // 清 CH 位，启动振荡
}
```
新买的/换过电池的 DS1302，CH 位默认是 1，**时钟不走**。必须开机时检查并清除。

#### 棘手点 3：写保护
DS1302 有个写保护寄存器（0x8E），置 0x80 后**禁止写入**。改时间前必须先关写保护，改完再开：
```c
write_byte(0x8E); write_byte(0x00);  // 关写保护
// ... 改时间 ...
write_byte(0x8E); write_byte(0x80);  // 开写保护
```

#### 自检函数 `DS1302_SelfTest`
开机时做 4 项检查：通信是否通、RAM 能否读写、写保护是否生效。返回 0=OK，-1/-2/-3 分别对应不同故障，通过串口打印出来，**方便你判断是接线问题还是芯片问题**。

---

### 5.8 `oled_ssd1306.c` —— 屏幕驱动 + 界面绘制

这个文件**最长最复杂**，因为它既要驱动屏幕硬件，又要画文字、画图形、画界面。

#### 5.8.1 屏幕的工作原理

SSD1306 是 128×64 像素的 OLED。像素按"页"组织：
```
共 8 页（page 0~7），每页高 8 像素，宽 128 像素。
framebuf[8][128] 数组就是整块屏幕的"显存"：
  framebuf[page][x] 的 8 个 bit，对应 (x, page*8)~(x, page*8+7) 这 8 个像素。
  bit=1 → 像素亮，bit=0 → 像素灭。
```

**画一个点**：
```c
static void oled_setpixel(uint8_t x, uint8_t y) {
    if (x > 127 || y > 63) return;
    framebuf[y / 8][x] |= (1 << (y % 8));  // 找到对应字节，置对应位
    dirty[y / 8] = 1;
}
```

#### 5.8.2 三套字体

| 字体 | 大小 | 用于 | 数据 |
|------|------|------|------|
| 5x7 | 5宽×7高 | 英文、数字（默认） | font.h |
| 16x16 | 16×16（占2页） | 大号数字、中文 | font.h |
| 12x12 | 12×12 | 主页紧凑中文菜单 | font.h |

#### 5.8.3 中文字符的"索引"机制

代码里不直接写"查"字，而是写它的**索引号**：
```c
enum { CN_CHA = 0, /* 查 */ CN_KAN, /* 看 */ ... };

// 画"查看心率"四个字：
uint8_t c1[] = {CN_CHA, CN_KAN, CN_XIN, CN_LV};
oled_draw_cn12_str(14, 26, c1, 4);
```
因为每个汉字要 24~32 字节字模，全字库太大塞不下，所以**只收录项目用到的 20 个汉字**。

#### 5.8.4 帧缓冲（framebuf）+ 脏标记（dirty）

不是每次画完都立刻传给屏幕，而是：
1. 先在 `framebuf` 里画好（纯内存操作，极快）。
2. 标记哪些页被改了（`dirty[page]=1`）。
3. `OLED_Flush()` 时只传被改的页到屏幕。

```c
void OLED_Flush(void) {
    for (uint8_t page = 0; page < 8; page++) {
        if (!dirty[page]) continue;        // 没改动就跳过，省时间
        write_cmd(0xB0 + page);            // 选页
        write_data(framebuf[page], 128);   // 传 128 字节
        dirty[page] = 0;
    }
}
```

#### 5.8.5 互斥锁保护（关键！）

`framebuf` 会被**两个任务**同时访问：Display 任务刷页面，Voice 任务被语音触发直接显示。如果同时操作，会花屏。

解决方案：每个 `OLED_Show*` 函数整段加锁：
```c
void OLED_ShowHeartRate(int hr) {
    if (oled_lock() != 0) return;   // 拿不到锁就放弃（不阻塞）
    OLED_Clear();
    // ... 画图 ...
    OLED_Flush();
    oled_unlock();
}
```

#### 5.8.6 特殊符号 °（度）的处理

5x7 字体表只有 ASCII 32~126，没有 °。代码用**像素拼**出一个 4×4 的小圆：
```c
oled_setpixel(dx + 1, dy + 0); oled_setpixel(dx + 2, dy + 0);  // 顶
oled_setpixel(dx + 0, dy + 1); oled_setpixel(dx + 3, dy + 1);  // 左右
...
```

#### 5.8.7 各个 Show* 页面布局

- **主页**：顶部大号时间（`HH:MM`） + 两侧菱形装饰 + 小号日期（5x7 居中） + 分隔线 + 6 条语音指令（双列 12x12 中文：查看心率 / 查看步数 / 归零步数 / 查看血氧 / 查看体温 / 查看时间）+ 底部分隔线。
- **心率页**：标题 `== Heart Rate ==` → 大号数字 → `bpm` → 参考范围 `Normal 60-100`（无信号时显示 `--` + `No signal`）。
- **血氧页**：类似心率页，单位 "%"。
- **步数页**：大号数字 → "steps" → 鼓励语。
- **体温页**：大号数字 + °C → 参考范围。无效时显示 "--.-"。

---

### 5.9 `hc05.c` —— 蓝牙通信

HC-05 是经典蓝牙模块，接在 UART1 上（和调试串口共用）。

#### 5.9.1 自适应波特率
```c
void HC05_Init(uint32_t baud) {
    if (hc05_try_baud(baud) == 0) return;  // 先试目标波特率
    // 不行就试备选：38400, 115200, 57600, 19200
    for (int i = 0; i < 4; i++) {
        if (hc05_try_baud(fallbacks[i]) == 0) {
            // 成功就发 AT 指令改成目标波特率
            snprintf(cmd, "AT+UART=%lu,0,0\r\n", baud);
            UART1_Init(baud);
            return;
        }
    }
}
```
**为什么这样？** HC-05 在 AT 模式（默认 38400）和透传模式（用户设的 9600）波特率不同。这个设计自动探测，不用你手动配置。

#### 5.9.2 接收指令解析
收到 `CAL` 开头 → 设置血氧校准系数；收到 `TIME` 开头 → 设置时间：
```c
if (strncmp(rxbuf, "CAL", 3) == 0) {
    float A = SPO2_CAL_A, B = SPO2_CAL_B;
    if (sscanf(rxbuf + 3, "%f %f", &A, &B) >= 1)
        Set_SpO2_Calibration(A, B);
}
```

#### 5.9.3 定时上报 JSON
```c
if ((now - last_report_tick) >= 2000) {  // 每 2 秒
    snprintf(txbuf, "{\"hr\":%d,\"spo2\":%d,\"steps\":%d,\"temp\":%.2f}\r\n", ...);
    UART1_Send(txbuf, n);
}
```
手机 App 收到这个 JSON 就能显示数据。**注意**：这里用轮询计时，没用 FreeRTOS 软件定时器（省内存）。

#### 5.9.4 清除 Overrun Error
```c
if (USART_GetFlagStatus(USART1, USART_FLAG_ORE) != RESET) {
    (void)USART_ReceiveData(USART1);  // 读 DR 清 ORE
}
```
STM32 的 UART 如果收到字节没及时读，会置 ORE 标志，**之后再也收不到新数据**。必须清除。

---

### 5.10 `asr_pro.c` —— 语音模块

ASR PRO 是个离线语音识别模块，识别到命令后通过 UART2 发一个**单字节指令码**。

#### 9 个指令码（`asr_pro.h`）

| 宏 | 值 | 触发后动作 |
|----|----|-----------|
| `ASR_CMD_HR` | 0x01 | 跳到心率页 |
| `ASR_CMD_STEPS` | 0x02 | 跳到步数页 |
| `ASR_CMD_STEPRST` | 0x03 | 步数清零 + 显示步数页 |
| `ASR_CMD_SPO2` | 0x04 | 跳到血氧页 |
| `ASR_CMD_TEMP` | 0x05 | 跳到体温页 |
| `ASR_CMD_TIME` | 0x06 | 跳到时间页 |
| `ASR_CMD_MAIN` | 0x07 | 跳到主页 |
| `ASR_CMD_NEXT` | 0x08 | 翻页（2 秒防抖） |
| `ASR_CMD_ACTIVITY` | 0x09 | 显示活动识别页（Walking/Running/Shaking/Resting） |

```c
while (USART_GetFlagStatus(USART2, USART_FLAG_RXNE) != RESET) {
    uint8_t c = USART_ReceiveData(USART2);
    switch (c) {
        case ASR_CMD_HR:      OLED_ShowHeartRate(Get_HeartRate()); break;
        case ASR_CMD_STEPS:   OLED_ShowSteps(Get_StepCount());     break;
        case ASR_CMD_STEPRST: Reset_StepCount(); OLED_ShowSteps(...); break;
        case ASR_CMD_SPO2:    OLED_ShowSpO2(Get_SpO2());           break;
        case ASR_CMD_TEMP:    OLED_ShowTemperature();             break;
        case ASR_CMD_TIME:    OLED_ShowTime();                     break;
        case ASR_CMD_MAIN:    OLED_ShowMainPage();                 break;
        case ASR_CMD_NEXT:    /* 翻页，带 2 秒防抖 */ break;
        case ASR_CMD_ACTIVITY:OLED_ShowActivity(Get_ActivityState());break;
    }
}
```

**NEXT 命令的防抖**：
```c
case ASR_CMD_NEXT: {
    uint32_t now = xTaskGetTickCount();
    if (now - last_next > pdMS_TO_TICKS(2000)) {  // 2 秒内只生效一次
        last_next = now;
        g_page_advance = 1;  // 通知 Display 任务翻页
    }
}
```
语音识别可能误触发或重复触发，防抖避免疯狂翻页。

---

### 5.11 按键与看门狗（已并入 `main.c`）

`keys.c` 和 `iwdg.c` 都已合并到 `main.c` 中，不再有独立文件。

#### 按键

只有一个按键 PA0（WK_UP），按下为高电平。

#### 特殊设计：开漏输出+低电平模拟下拉
```c
cfg.GPIO_Mode = GPIO_Mode_Out_OD;       // 开漏输出
GPIO_ResetBits(GPIOA, GPIO_Pin_0);      // 输出低 = 强下拉
```
**为什么不直接配成输入？** 因为 WK_UP 按键接 VCC，需要下拉。开发板可能没装下拉电阻，用开漏输出低电平"充当"下拉。**这是一个硬件妥协**。

#### 软件消抖
```c
if (now - *last_tick <= pdMS_TO_TICKS(300)) return 0;  // 300ms 防连按
```
调用周期 50ms，远大于机械抖动（<10ms），不用额外消抖，只防连按。

---

#### 独立看门狗（IWDG）

已并入 `main.c`。按键内容结束后原本的 `iwdg.c` 小节整体替换为如下精简内容。

```c
void IWDG_Init(void) {
    IWDG_WriteAccessCmd(IWDG_WriteAccess_Enable);
    IWDG_SetPrescaler(IWDG_Prescaler_256);   // 40kHz / 256 ≈ 156 Hz
    IWDG_SetReload(625);                      // 625 / 156 = 4.0 秒
    IWDG_ReloadCounter();                     // 先喂一次
    IWDG_Enable();                            // 启动（不可逆）
}
```
**选 4 秒的原因**：正常任务周期 200ms，余量 20 倍。即使某次 I2C 重试拉长到 1~2 秒也不会误触发。只有真死机才会超时。

---

### 5.13 `systick.c` —— 时间工具

提供微秒/毫秒延时和获取系统 tick。

#### 用 DWT 做微秒级精确延时
```c
void Delay_us(uint32_t us) {
    uint32_t start  = DWT->CYCCNT;                    // 读当前周期计数
    uint32_t target = us * (SystemCoreClock / 1000000u);  // 换算成周期数
    while ((DWT->CYCCNT - start) < target) {}         // 死等
}
```
**DWT**（Data Watchpoint and Trace）是 Cortex-M3 内置的周期计数器，72MHz 下每周期 13.9ns，**比任何定时器都精确**。

**为什么 `CYCCNT - start` 能正确处理溢出？** 因为 uint32_t 减法溢出会自动回绕，数学上结果正确（只要等待时间 < 59.6 秒，即 2^32 周期）。

#### `Systick_GetTick` 的双模式
```c
if (xTaskGetSchedulerState() != taskSCHEDULER_NOT_STARTED) {
    return xTaskGetTickCount();          // 调度器启动后用 FreeRTOS tick
}
return DWT->CYCCNT / (SystemCoreClock / 1000u);  // 启动前用 DWT
```
因为 FreeRTOS tick 在调度器启动前是 0，得用 DWT 兜底（比如 HC05_Init 在 main 里调用，那时调度器还没启动）。

---

## 第 6 章 逐文件精讲（.h 头文件）

头文件是"声明"和"配置"，相对简单。

### 6.1 配置参数（算法参数在 `algorithms.h`，硬件参数在各传感器 `.h`）

重构后已取消独立的 `*_config.h` 文件。算法参数统一放在 `algorithms.h`，各传感器的硬件配置（I2C 地址、引脚等）放在各自 `.h` 里：

| 文件 | 参数 |
|------|------|
| `algorithms.h` | **PPG**（`PPG_SAMPLE_RATE` / `PPG_BUF_LEN` 等）、**计步**（`STEP_GRAVITY_EMA` / `STEP_MIN_INTERVAL_MS` 等）、**SpO2** 校准（`SPO2_CAL_A` / `SPO2_CAL_B`）、**体温补偿**（`TEMP_BASE_OFFSET` / `TEMP_AMBIENT_COEFF` 等） |
| `max30102.h` | I2C 地址 `0xAE`、LED 电流、FIFO 模式等寄存器配置 |
| `mpu6050.h` | I2C 地址 `0xD0`、加速度量程、芯片温度计算公式参数 |
| `ds18b20.h` | 1-Wire 引脚 PA1 |
| `ds1302.h` | 三线引脚：CLK=PB1、DAT=PB14、CE=PB13 |

### 6.2 接口声明类头文件

`max30102.h` `mpu6050.h` `ds18b20.h` `ds1302.h` `oled_ssd1306.h` `hc05.h` `asr_pro.h` `i2c.h` `uart.h` `systick.h` `algorithms.h` `globals.h`

这些就是"菜单"，列出对外可用的函数。比如 `algorithms.h`：
```c
void PPG_Init(void);
int  Get_HeartRate(void);
int  Get_SpO2(void);
void Reset_StepCount(void);
...
```

### 6.3 字体类头文件

`font.h`（合并后的字库，含 `font5x7` ASCII + `font_cn_12` 12x12 中文 + 索引枚举）。

这些是**数据表**，不是逻辑。字体是 Python 脚本从 simhei.ttf 渲染生成的（见文件头注释）。

### 6.4 `FreeRTOSConfig.h` —— RTOS 配置

关键配置：
```c
configCPU_CLOCK_HZ    = 72000000   // CPU 主频
configTICK_RATE_HZ    = 1000       // 1ms 一个 tick
configMAX_PRIORITIES  = 5          // 优先级 0~4
configTOTAL_HEAP_SIZE = 15 * 1024  // FreeRTOS 堆 15KB
configCHECK_FOR_STACK_OVERFLOW = 1 // 开启栈溢出检测
configUSE_MUTEXES     = 1          // 用互斥锁
configUSE_TIMERS      = 0          // 不用软件定时器（省 1.2KB）
```

### 6.5 `globals.h` —— 全局共享变量

```c
extern volatile float g_temperature;   // 体温
extern volatile int   g_temp_valid;    // 体温有效
extern volatile int   g_page_advance;  // 翻页请求
void Get_Temperature(int *valid, float *temp);  // 原子读取
```
跨任务通信的"公共布告栏"。

---

## 第 7 章 核心难点：计步、心率、血氧算法

这一章讲 `algorithms.c`，**全项目最难的算法逻辑**。

### 7.1 计步算法（Step_ProcessAccel）

#### 思路
人走路时，加速度的**模**（√(x²+y²+z²)）会周期性波动。检测这个波动峰值就能计步。

#### 但难点是：怎么区分"走路"和"抖一下"？

本项目用了**多层过滤**：

**第 1 层：去重力**
```c
float mag = sqrtf(ax*ax + ay*ay + az*az);  // 加速度模
grav = 0.98*grav + 0.02*mag;               // EMA 估计重力基线
float acc = mag - grav;                    // 减掉重力，得到"净加速度"
```
EMA（指数移动平均）是个低通滤波器，慢慢跟踪重力变化。

**第 2 层：自适应阈值**
```c
thr = 0.995*thr + 0.005*fabsf(acc);  // 阈值跟着活动强度变
```
走路时 acc 波动大，阈值自动调高；静止时波动小，阈值调低（但有下限 200）。

**第 3 层：滞回检测（关键！）**
```c
if (!step_armed) {
    if (acc > eff_thr) step_armed = 1;     // 越过阈值 → 武装
} else {
    if (acc < eff_thr * 0.5f) {            // 跌回一半阈值 → 确认一步
        step_armed = 0;
        // ... 计步 ...
    }
}
```
**为什么用滞回？** 如果只用"acc>阈值就+1"，一个宽峰会被计多次（波峰在阈值上方抖动）。滞回要求"先上去再下来"才算一步。

**第 4 层：步间隔检查**
```c
if ((now - step_last) > STEP_MIN_INTERVAL_MS) {  // 300ms 内不算
```
人最快也就 200 步/分钟（5 步/秒），间隔 < 300ms 的肯定是噪声。

**第 5 层：步态连续性确认（防"碰一下"）**
```c
// 前几步先存暂存队列 step_pending_ts[]
// 只有 3 秒内凑齐 ≥2 步，才确认是"连续步态"，回补计入 steps
```
**这是精华**：碰一下桌子、拍一下手，都是孤立单次扰动，凑不齐连续步态，被过滤掉。真走路是连续的，能凑齐。

#### 运动状态分类
根据步频（步/分钟）分类：
- spm > 120：跑步
- spm > 40：走路
- 否则：未知

长时间没动作但晃动能量大：识别为"摇晃（Shaking）"。

### 7.2 心率算法（PPG_ProcessSamples + compute_hr）

PPG = 光电容积脉搏波。MAX30102 测的是光强，需要从中提取心跳。

#### 步骤 1：滤波
原始信号有低频漂移（呼吸/温度）和高频噪声，心跳在 0.5~5Hz 之间。
```c
// 高通滤波（去低频漂移）
yhp = alpha_hp * (prev_hp + x - prev_x);
// 低通滤波（去高频噪声）
ylp = prev_lp + alpha_lp * (yhp - prev_lp);
```
这是经典的一阶 IIR 滤波器。

#### 步骤 2：找波峰
```c
float thresh = mean + 0.8 * stdv;  // 阈值 = 均值 + 0.8 倍标准差
for (i = 1; i < N-1; i++) {
    if (filtered[i] > thresh &&
        filtered[i] > filtered[i-1] &&
        filtered[i] > filtered[i+1]) {
        add_peak(abs_idx);  // 这是一个心跳波峰
    }
}
```

#### 步骤 3：算心率
```c
// 取最近几个峰间间隔
for (k) iv[k] = peak_at(k+1) - peak_at(k);
// 插入排序求中位数 median
// 只取 median±20% 范围内的间隔求平均（去离群）
avg_period = sum / cnt;
hr = 60 / avg_period;  // 心率 = 60 / 周期(秒)
```
**中值过滤**：漏检一个峰会使间隔翻倍，误检一个峰会使间隔减半，都是离群值，用 median±20% 过滤掉。

#### 步骤 4：信号质量门控
```c
int signal_good = (stdv >= 0.2) && (mean_ir >= 5000);
```
- `stdv >= 0.2`：有脉搏波纹（不是平的）
- `mean_ir >= 5000`：有手指按压（IR 被组织吸收，直流分量高）

两者都满足才算有效。还要**连续 4 个窗口**都好才"锁定"输出，过滤短暂扰动。

#### 步骤 5：EMA 平滑 + 变化率限幅
```c
if (target > ema_hr + 10) target = ema_hr + 10;  // 单次最多变 10 bpm
ema_hr = 0.15 * target + 0.85 * ema_hr;          // 指数平滑
```
防止心率读数跳变。

### 7.3 血氧算法（SpO₂）

```c
float R = (std_red/mean_red) / (std_ir/mean_ir);  // 比值
int est = spo2_cal_A - spo2_cal_B * R;             // 经验公式
// est = 110 - 25*R
```
**原理**：血氧饱和度影响红光和红外光的吸收比。R 值越大，血氧越低。公式 `110 - 25R` 是经验拟合，可蓝牙校准（`CAL` 指令改 A、B）。

结果限制在 50~100 之间。

---

## 第 8 章 所有可调参数速查表

想调效果？改这些宏定义（在 `crouse.h/` 的文件里）：

### 心率/血氧（algorithms.h，原 max30102_config.h）
| 参数 | 默认 | 含义 | 调它的影响 |
|------|------|------|-----------|
| PPG_SAMPLE_RATE | 100Hz | 采样率 | 改了要同步改算法系数 |
| PPG_STD_ACTIVE_MIN | 0.2 | "有信号"阈值 | 调低→灵敏但易误判 |
| PPG_IR_DC_MIN | 5000 | "有手指"阈值 | 调低→没贴紧也显示 |
| PPG_LOCK_COUNT | 4 | 锁定所需好窗口数 | 调大→更稳但出值慢 |
| PPG_BAD_HOLD_MAX | 3 | 多少坏窗口才归零 | 调大→读数更持久 |
| HR_MAX_DELTA | 10 | 心率单次最大变化 | 调小→更稳但跟不上 |
| SPO2_CAL_A/B | 110/25 | 血氧校准 | 用 CAL 指令动态改 |

### 计步（algorithms.h，原 mpu6050_config.h）
| 参数 | 默认 | 含义 |
|------|------|------|
| STEP_MIN_THRESH | 200 | 阈值下限，抗静止噪声 |
| STEP_MIN_INTERVAL_MS | 300 | 最小步间隔 |
| STEP_CONFIRM_MIN | 2 | 凑齐几步才算连续步态 |
| STEP_VALID_WINDOW_MS | 3000 | 连续步态时间窗 |
| ACTIVITY_WALK_SPM_MIN | 40 | 步行步频下限 |
| ACTIVITY_RUN_SPM_MIN | 120 | 跑步步频下限 |

### 体温（algorithms.h，原 ds18b20_config.h）
| 参数 | 默认 | 含义 |
|------|------|------|
| TEMP_BASE_OFFSET | 3.0 | 皮肤→核心体温基础补偿 |
| TEMP_AMBIENT_COEFF | 0.05 | 环境温度影响系数 |
| TEMP_OFFSET_MIN/MAX | 1.5/4.0 | 补偿量上下限 |

### 显示（oled_ssd1306.c / main.c）
| 参数 | 默认 | 含义 |
|------|------|------|
| DISPLAY_REFRESH_CYCLES | 10 | 刷新间隔（10×50ms=500ms） |
| HC05_REPORT_INTERVAL_MS | 2000 | 蓝牙上报间隔 |

---

## 第 9 章 编译、烧录、调试

### 9.1 用 Keil MDK 编译

1. 打开 `TASK1.uvprojx`。
2. 编译器是 **AC6（Armclang）**，已配置好。
3. 点 Build（F7）。应该 0 error。
4. 连接 ST-Link/J-Link，点 Download（F8）烧录。

### 9.2 串口调试

- 用 USB-TTL 模块接 PA9（TX）、PA10（RX）、GND。
- 打开串口工具（如 SSCOM、PuTTY），波特率 **115200**（开机阶段）或 **9600**（HC-05 初始化后）。
- 开机后会看到：
  ```
  DS1302: OK
  RTC: 12:30:45 23/06/2026
  ```
  如果是 `NO COMM`，检查 DS1302 接线。

### 9.3 蓝牙连接

- 手机装蓝牙串口 App（如"蓝牙调试器"）。
- 搜索 "HC-05"，配对码通常 `1234` 或 `0000`。
- 连上后每 2 秒收到：
  ```json
  {"hr":72,"spo2":98,"steps":1234,"temp":36.50}
  ```
- 发送 `TIME12:30:00 23/6/26` 可对时。
- 发送 `CAL31 20` 可校准血氧。

### 9.4 语音模块配置

ASR PRO 需要预先烧录命令词，让它识别后发送对应字节码（0x01~0x09）。用"智能管家"或"天问Block"软件配置，命令对应关系见 `asr_pro.h`。

### 9.5 常见问题

| 现象 | 可能原因 |
|------|----------|
| 屏幕全黑 | I2C2 接线错 / OLED 地址不是 0x3C（试 0x3D） |
| 心率一直 0 | 手指没贴紧 / MAX30102 没初始化（查 I2C1） |
| 步数乱跳 | MPU6050 没固定好 / STEP_MIN_THRESH 太低 |
| 体温不对 | DS18B20 没接上拉电阻 / 补偿参数要调 |
| 频繁重启 | 看门狗超时 → 某个任务卡死（看串口 ASSERT 信息） |
| 蓝牙连不上 | HC-05 波特率不对，看串口有没有 AT 响应 |

---

## 第 10 章 已知问题与改进方向

### 当前状态
项目代码质量较高，注释详尽，没有会导致崩溃的严重 bug。以下是可选的改进点：

1. **UART 接收用轮询**：如果未来加更多串口设备，建议改中断/DMA 接收，避免漏数据。
2. **`ir_dec`/`red_dec` 在栈上（256 字节）**：可改 `static` 进一步降低栈占用（当前余量够，非必需）。
3. **`g_page_advance` 读清零非严格原子**：当前有 2 秒防抖保护，无害；追求严谨可加临界区。
4. **没有持久化存储**：步数、校准参数掉电丢失。可写入 DS1302 的 RAM 或加片内 Flash 存储。
5. **血氧校准是线性的**：真实 R-SpO₂ 关系是非线性的，精度有限。
6. **字体硬编码**：加新汉字要重新生成字模表。

### 可扩展方向
- 加 WiFi（ESP8266）上传到云端
- 加 SD 卡记录历史数据
- 加陀螺仪姿态解算（MPU6050 的陀螺仪部分没用到）
- 加低功耗模式（现在一直全速跑，电池不耐用）

---

## 附录 A：术语表

| 术语 | 解释 |
|------|------|
| MCU | 单片机，集成 CPU+内存+外设的芯片 |
| GPIO | 通用输入输出引脚 |
| I2C | 两线串行通信协议 |
| UART | 串口，异步串行通信 |
| 1-Wire | 单线通信协议 |
| SPI | 串行外设接口（本项目没用） |
| FIFO | 先进先出缓存（MAX30102 里存采样数据） |
| ISR | 中断服务函数 |
| RTOS | 实时操作系统（本项目用 FreeRTOS） |
| Task | RTOS 中的任务/线程 |
| 临界区 | 关中断的代码段，不可被打断 |
| 互斥锁 | 保护共享资源的机制 |
| PPG | 光电容积脉搏波（测心率血氧的原理） |
| BCD | 二进制编码的十进制（DS1302 用） |
| EMA | 指数移动平均（一种平滑滤波） |
| IIR | 无限脉冲响应（一种数字滤波器） |
| DWT | Cortex-M 的调试跟踪单元（含周期计数器） |
| IWDG | 独立看门狗 |
| LSB | 最低有效位 / 传感器量化的最小单位 |
| JSON | 一种文本数据格式（蓝牙上报用） |

---

## 附录 B：文件清单

```
最小开发板/
├── TASK1.uvprojx              # Keil 工程文件
├── 项目技术文档.md            # 本文档
├── crouse.c/                  # 源文件目录
│   ├── main.c                 # 主程序 + 4 个任务（含按键/看门狗）
│   ├── algorithms.c           # 心率/血氧/计步算法
│   ├── oled_ssd1306.c         # OLED 驱动 + 界面
│   ├── i2c.c                  # I2C1/I2C2 硬件抽象
│   ├── uart.c                 # UART1/UART2 硬件抽象
│   ├── max30102.c             # 心率血氧传感器驱动
│   ├── mpu6050.c              # 加速度计驱动
│   ├── ds18b20.c              # 温度传感器驱动
│   ├── ds1302.c               # 实时时钟驱动
│   ├── hc05.c                 # 蓝牙通信
│   ├── asr_pro.c              # 语音识别处理
│   └── systick.c              # 延时/计时
├── crouse.h/                  # 头文件目录
│   ├── algorithms.h           # API + 所有算法参数
│   ├── max30102.h / mpu6050.h / ds18b20.h / ds1302.h  # 各传感器驱动+硬件配置
│   ├── i2c.h / uart.h / hc05.h / asr_pro.h / systick.h  # 通信/外设接口
└── (FreeRTOS/StdPeriph 等库文件)
```

---

**文档完。**

如果你是第一次看这个项目，建议按这个顺序学习：
1. 先读第 0~3 章，建立整体认识。
2. 跑起来程序，用串口看调试输出。
3. 读 `main.c`，理解开机流程和 4 个任务。
4. 挑一个传感器（建议从 `mpu6050.c` 开始，最简单），配合算法看数据流。
5. 最后啃 `algorithms.c` 和 `oled_ssd1306.c` 这两个硬骨头。

祝玩得开心！🚀
