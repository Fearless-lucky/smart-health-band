# STM32F103ZE 智能健康手环 — 接线示意图

> 本接线图与 `crouse.c/` 源码、`crouse.h/*_config.h` 配置文件**逐项核对一致**。
> 修改任何引脚时请同步更新：本文件、`README.md`、对应 `*_config.h`、`i2c_hal.c`、`keys.c`、`uart_hal.c`。

```
STM32F103ZE                               外设模块
 ┌─────────────────────────────────┐
 │                                 │
 │  PA0 ───────────────────────────┬──── 按键一端 (WK_UP, 翻页)
 │                                 │    开漏输出强下拉, 按下接 VCC→高电平
 │                                 │    (按键另一端接 3.3V)
 │                                 │
 │  PA1 ───────────────────────────┬──── DQ  │  DS18B20
 │                                 │    (4.7kΩ 上拉到 3.3V, 必需!)
 │                                 │
 │  PA2 ───────────────────────────┬──── RX  │  ASR PRO 语音模块
 │  PA3 ───────────────────────────┬──── TX  │  (UART2, 9600bps)
 │                                 │
 │  PA9 ───────────────────────────┬──── RX  │  HC-05 蓝牙模块
 │  PA10 ──────────────────────────┬──── TX  │  (UART1, 9600bps, 兼调试)
 │                                 │
 │  PB1 ───────────────────────────┬──── CLK │
 │  PB13 ──────────────────────────┬──── CE  │  DS1302 RTC
 │  PB14 ──────────────────────────┬──── DAT │  (三线 SPI-like)
 │                                 │
 │  PB8 ───────────────────────────┬──── SCL │  ╔══════════════════════╗
 │  PB9 ───────────────────────────┬──── SDA │  ║  I2C1 总线 (100kHz)  ║
 │                                 │         │  ║  ⚠️ I2C1 重映射       ║
 │                                 │         │  ║  默认 PB6/PB7→PB8/PB9║
 │                                 │         │  ╠══════════════════════╣
 │                                 │         │  ║ MAX30102 (0x57)      ║
 │                                 │         │  ║  心率血氧 PPG 传感器  ║
 │                                 │         │  ║ VIN→3.3V  GND→GND    ║
 │                                 │         │  ╠══════════════════════╣
 │                                 │         │  ║ MPU6050  (0x68)      ║
 │                                 │         │  ║  六轴加速度+陀螺仪    ║
 │                                 │         │  ║ VCC→3.3V  GND→GND    ║
 │                                 │         │  ╚══════════════════════╝
 │  PB10 ──────────────────────────┬──── SCL │  ╔══════════════════════╗
 │  PB11 ──────────────────────────┬──── SDA │  ║  I2C2 总线 (100kHz)  ║
 │                                 │         │  ╠══════════════════════╣
 │                                 │         │  ║ SSD1306 OLED (0x3C)  ║
 │                                 │         │  ║  128×64 单色显示屏   ║
 │                                 │         │  ║ VCC→3.3V  GND→GND    ║
 │                                 │         │  ╚══════════════════════╝
 │                                 │
 │  3.3V ──────────────────────────┬─────────┤  ├─ 大部分模块 VCC
 │  5V   ──────────────────────────┬─────────┤  ├─ ASR PRO 模块 VCC (5V)
 │  GND  ──────────────────────────┬─────────┤  └─ 所有模块 GND
 └─────────────────────────────────┘
```

---

## 引脚速查表

| STM32 引脚 | 功能 | 模块 | 通信协议 | 速率 | 备注 |
|-----------|------|------|---------|------|------|
| **PA0** | 翻页按键 (WK_UP) | 轻触按键 | GPIO | — | 开漏输出强下拉, 按下→高电平 (接 3.3V) |
| **PA1** | DQ | DS18B20 | 1-Wire | — | **⚠️ 必须外接 4.7kΩ 上拉至 3.3V** |
| **PA2** | TX | ASR PRO | UART2 | 9600 | STM32 TX → 模块 RX |
| **PA3** | RX | ASR PRO | UART2 | 9600 | STM32 RX → 模块 TX |
| **PA9** | TX | HC-05 | UART1 | 9600 | 启动瞬间 115200 探测, 稳态 9600 |
| **PA10** | RX | HC-05 | UART1 | 9600 | STM32 RX → 模块 TX |
| **PB1** | CLK | DS1302 | 三线 SPI | — | 软件模拟时序 |
| **PB8** | SCL | MAX30102 + MPU6050 | I2C1 | 100kHz | **⚠️ I2C1 重映射脚**, 需 4.7kΩ 上拉 |
| **PB9** | SDA | MAX30102 + MPU6050 | I2C1 | 100kHz | **⚠️ I2C1 重映射脚**, 需 4.7kΩ 上拉 |
| **PB10** | SCL | SSD1306 OLED | I2C2 | 100kHz | 独占总线, 需 4.7kΩ 上拉 |
| **PB11** | SDA | SSD1306 OLED | I2C2 | 100kHz | 独占总线, 需 4.7kΩ 上拉 |
| **PB13** | CE (RST) | DS1302 | 三线 SPI | — | 软件模拟时序 |
| **PB14** | DAT | DS1302 | 三线 SPI | — | 软件模拟时序 |

> **I2C1 重映射说明**：STM32F103 的 I2C1 默认引脚是 PB6(SCL)/PB7(SDA)。本项目通过
> `GPIO_PinRemapConfig(GPIO_Remap_I2C1, ENABLE)`（见 `i2c_hal.c`）将其重映射到 PB8/PB9，
> 避免与板上其他外设冲突。**接线必须用 PB8/PB9，而非 PB6/PB7**。

---

## 关键接线注意事项

### 🔴 DS18B20 上拉电阻（必须！）
**PA1 与 3.3V 之间必须焊接 4.7kΩ 上拉电阻。** 1-Wire 总线依赖上拉提供空闲高电平，缺少上拉将导致设备永远不被检测到（`DS18B20_Init()` 返回 -1）。

DS18B20 接线：
```
  3.3V ───╦─── VDD (DS18B20 引脚1)
          │
         4.7kΩ
          │
  PA1 ───╦─── DQ  (DS18B20 引脚2)
         │
         └─── 也可只用两线模式 (VDD 接 GND, 寄生供电)
              但本项目推荐三线模式 (VDD 接 3.3V, 稳定性更好)

  GND ─────── GND (DS18B20 引脚3)
```

### 🟡 I2C 上拉电阻
I2C1 (PB8/PB9) 和 I2C2 (PB10/PB11) 的 GPIO 配置为 `GPIO_Mode_AF_OD`（开漏），**依赖外部或模块自带的上拉电阻**。空闲时 SCL/SDA 应为 3.3V，若测得 0V 说明上拉缺失或某设备把总线拉死。

- 大多数 MAX30102 / MPU6050 / OLED 模块板已自带 4.7kΩ 上拉，可直连；
- 使用裸芯片时，PB8/PB9/PB10/PB11 各需一颗 4.7kΩ 上拉到 3.3V。

### 🟢 电源
- STM32F103ZE 工作电压 2.0–3.6V，本项目使用 **3.3V**
- MAX30102 需要 1.8V（内核）+ 3.3V（LED），模块板通常自带 LDO
- MPU6050 / DS1302 / SSD1306 / HC-05 均为 3.3V
- **ASR PRO 语音模块通常为 5V 供电**（注意区分），其 TX 输出多为 3.3V 电平，可直接接 STM32 RX；若实测 TX 高电平 >3.6V，建议串 1kΩ 限流
- DS18B20 宽电压 3.0–5.5V，3.3V 下正常工作

### 🔵 翻页按键
- 仅 **1 个按键**，接 PA0（WK_UP）
- STM32 端 PA0 配置为开漏输出并输出低电平（强下拉），按键另一端接 3.3V
- 按下 → PA0 被拉到高电平 → 触发翻页（`Key_Get()` 高电平有效）
- 软件已做 300ms 防连按，无需硬件消抖电容

### ⚪ HC-05 模块配置要求
HC-05 上电时，`HC05_Init()` 自动尝试波特率链：**9600 → 38400 → 115200 → 57600 → 19200**。
- 若在 9600 收到 `OK` → 直接使用 9600
- 若在其它波特率收到 `OK` → 自动发 `AT+UART=9600,0,0` 将模块重配为 9600
- 因此 HC-05 模块**首次可用任意波特率**（上述 5 种之一），代码会自适配到 9600

> 如需修改 HC-05 其它参数（名称、配对码），让 HC-05 在未配对状态上电（STATE 灯慢闪），用 USB-TTL 直连 HC-05 发送 AT 命令。
